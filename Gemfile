source "https://rubygems.org"

# Local preview only — GitHub Pages builds with its own pinned toolchain.
gem "jekyll", "~> 4.3"
gem "jekyll-seo-tag"
gem "jekyll-sitemap"
gem "webrick" # needed for `jekyll serve` on Ruby >= 3
